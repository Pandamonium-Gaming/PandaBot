name: .NET CI/CD

on:
  push:
    branches: [ "main" ]   # run when pushing to main branch
  pull_request:
    branches: [ "main" ]   # run when there's a PR to main branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Get code from repo
      - name: Checkout code
        uses: actions/checkout@v3

      # Install .NET SDK (example .NET 9, change version according to your project)
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      # Restore packages
      - name: Restore dependencies
        run: dotnet restore src/PandaBot

      # Build project
      - name: Build
        run: dotnet build src/PandaBot --configuration Release --no-restore

      # Run tests (if there's a separate test project)
      # - name: Test
      #   run: dotnet test tests/PandaBot.Tests --no-build --verbosity normal

      # Publish to artifacts directory
      - name: Publish
        run: dotnet publish src/PandaBot -c Release -o artifacts/PandaBot

      # Upload artifact to download from GitHub Actions
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: PandaBot
          path: artifacts/PandaBot
